export const addTriggerSettingsPanel = (triggerType,source,level,slope,when,windowTop, windowBottom,
    edge,pattern,triggerWhen) => ({
    type: 'TRIGGERPANEL', triggerType,source,level,slope,when,windowTop,windowBottom,edge,pattern,triggerWhen
    });
    
    